const Error = ({ children }) => {
  return (
    <div className="uppercase bg-red-700 text-white text-center p-3 font-bold mb-3 rounded-md">
        {children}
    </div> 

  )
}

export default Error